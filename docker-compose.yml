version: "3"
services:
  postgres:
    image: postgres:15
    restart: always
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: flow
      POSTGRES_PASSWORD: flow
      POSTGRES_DB: flowdb
    volumes:
      - postgres_data:/var/lib/postgresql/data
volumes:
  postgres_data: